/**
 * The scenarios this application publishes to `lanka`.
 *
 * The CLASS, not the instance: the locator builds what it is given, and the
 * instance beside the class is what the application triggers directly.
 */
export { AtlasMissionCompleted } from "@lanka-playgrounds/_shared/di";
