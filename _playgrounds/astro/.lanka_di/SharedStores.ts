/**
 * The shared stores this application publishes to `lanka`.
 *
 * Empty, deliberately: a shared store is module state, and this application
 * renders on a server where module state is shared by every reader.
 */
export {};
