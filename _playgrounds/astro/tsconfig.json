{
	"//paths": "`.lanka_di` and not `.lanka` ON PURPOSE. Both names are legal and `@lankajs/tool-di` resolves either; every other application here is on `.lanka`, the name a new project gets, and this one carries the second name so that it stays proved under a real build rather than only under unit tests with temporary directories. Renaming it for consistency is the change that silently drops that coverage — `scripts/check-playgrounds.mjs` refuses it, and README.md says why.",
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"noEmit": true,
		"paths": {
			"@lanka_di/*": ["./.lanka_di/*"]
		}
	},
	"include": ["src/**/*", ".lanka_di/**/*", "vitest.config.ts"]
}
