/**
 * An application's entry, as small as one can be while still loading lanka and
 * reading one of its own barrels — which is what the Vite 8 scene builds.
 */
export { startLanka } from "lanka/bootstrap";
export { lankaHost } from "@lanka_di/Host";
