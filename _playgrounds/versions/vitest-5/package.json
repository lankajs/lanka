{
	"name": "@lanka-playgrounds/versions-vitest-5",
	"private": true,
	"type": "module",
	"description": "The test kit under Vitest 5: its setup file, its helpers and a conformance suite registering into the running Vitest, and a Vite 8 build through tool-di.",
	"scripts": {
		"lint": "eslint src --max-warnings=0",
		"test": "vitest run",
		"//test:coverage": "The suite without --coverage: its subject is the test kit, measured by its own suite. What this application adds is the MAJOR of Vitest it runs under.",
		"test:coverage": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@lanka-playgrounds/versions-shared": "workspace:^",
		"@lankajs/tool-di": "workspace:^",
		"@lankajs/tool-testing": "workspace:^",
		"lanka": "workspace:^",
		"zustand": "^5.0.10"
	},
	"devDependencies": {
		"vite": "^8.3.0",
		"vitest": "^5.0.1"
	}
}
