{
	"name": "@lanka-playgrounds/versions-angular-22",
	"private": true,
	"type": "module",
	"description": "The Angular binding against Angular 22: every scene the binding is held to, and its testing entry with @testing-library/angular 19.5.0.",
	"scripts": {
		"lint": "eslint src --max-warnings=0",
		"test": "vitest run",
		"//test:coverage": "The suite without --coverage: this application owns no source — its scenes live in versions/_shared and its subject is a workspace package measured by its own suite. What it adds is the MAJOR it runs against.",
		"test:coverage": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@lanka-playgrounds/versions-shared": "workspace:^",
		"@lankajs/tool-testing": "workspace:^",
		"lanka": "workspace:^",
		"zustand": "^5.0.10"
	},
	"devDependencies": {
		"@angular/common": "^22.2.0",
		"@angular/compiler": "^22.2.0",
		"@angular/core": "^22.2.0",
		"@angular/platform-browser": "^22.2.0",
		"@angular/router": "^22.2.0",
		"@testing-library/angular": "^19.5.0",
		"@testing-library/dom": "^10.4.2",
		"jsdom": "^27.4.0",
		"rxjs": "^7.8.2"
	}
}
