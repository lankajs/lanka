/**
 * The gateways this application publishes to `lanka`.
 *
 * From the NARROW barrel: the framework imports this file from inside
 * `lanka/locator`, so whatever it reaches for is pulled in while the locator is
 * still evaluating.
 */
export { AtlasMissionGateway } from "@lanka-playgrounds/_shared/di";
