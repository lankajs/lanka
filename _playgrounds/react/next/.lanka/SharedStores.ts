/**
 * The shared stores this application publishes to `lanka`.
 *
 * Empty, and that is a decision rather than an omission: a shared store is
 * module state, and module state on a server is one store shared by every
 * request. The dispatch draft lives in the browser application, not here.
 */
export {};
