/**
 * The playground's entry point: one path a scene starts from.
 *
 * A project as a consumer's actually looks — core, a plugin, the test kit and
 * React — and nothing else, because what the scenes prove is about the SET the
 * command sees rather than about any one package.
 */
export { createPlaygroundProject } from "./create-playground-project/createPlaygroundProject";
export { playgroundRoot } from "./playground-root/playgroundRoot";
