/**
 * The consumer's project root, as every scene refers to it.
 *
 * Its own file rather than a second export beside the project: one file, one
 * runtime identity, and a path every scene shares is a subject of its own.
 */
export const playgroundRoot = "/app";
