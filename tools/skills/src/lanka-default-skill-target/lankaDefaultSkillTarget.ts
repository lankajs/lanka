/**
 * Where a project's agent skills live, unless the caller says otherwise.
 *
 * A default rather than a required argument: the folder is the same in almost
 * every project, and an argument everybody passes identically is a question
 * nobody was asking.
 */
export const lankaDefaultSkillTarget = ".claude/skills";
