/**
 * Where the imaginary project is.
 *
 * A constant rather than a literal in each scene: every assertion in this
 * playground is about a PATH, and a scene that spelled the root itself would
 * pass while pointing somewhere the command never wrote.
 */
export const playgroundRoot = "/app";
