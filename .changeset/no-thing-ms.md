---
"@lankajs/plugin-prefetch": major
---

`thingMs` is gone; the pause between two chunks of the sweep is `betweenChunksMs` only. 2.1.0 read the old name as an alias for one release; carrying a name that said nothing forward was not worth a compatibility branch nobody should be on.
