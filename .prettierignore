# Generated documentation. `scripts/scaffold.mjs` writes it from
# `scripts/registry.mjs`, and reformatting it is pointless: the next scaffolder
# run restores its own shape and the drift check sees the difference. Of two
# owners a file keeps one — the generator.
/README.md
/CLAUDE.md
core/README.md
core/package.json
core/tsconfig.json
core/tsup.config.ts
modules/*/README.md
modules/*/package.json
modules/*/tsconfig.json
modules/*/tsup.config.ts
plugins/*/README.md
plugins/*/package.json
plugins/*/tsconfig.json
plugins/*/tsup.config.ts
tools/*/README.md
tools/*/package.json
tools/*/tsconfig.json
tools/*/tsup.config.ts

# Written by `scripts/check-api.mjs --write` and `check-perf.mjs --write`, and
# compared with them. Two reports were reformatted once by a wildcard
# `prettier --write`, and the surface check then reported a divergence nobody had
# caused.
api/
perf/

# Generated by the scaffolder and compared byte for byte — formatting would break
# the comparison, and with it the point of the check.
tools/testing/fixtures/.lanka_di/

node_modules/
dist/
coverage/
